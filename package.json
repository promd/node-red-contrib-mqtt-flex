{
  "name": "node-red-contrib-mqtt-dynamic",
  "version": "0.1.1",
  "description": "Set of Nodes for NodeRed that maintains multiple MQTT connections at the same time. Connections can be configured and created on run-time.",
  "dependencies": {
    "mqtt": "^4.2.8"
  },
  "main": "mqtt-dynamic.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node-red"
  ],
  "node-red": {
    "nodes": {
      "mqtt-dynamic": "mqtt-dynamic.js"
    }
  },
  "author": "",
  "license": "ISC"
}
